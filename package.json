{
  "scripts": {
    "build": "esbuild main.js --bundle --outfile=dist/app.bundle.js --format=iife --platform=browser --target=chrome80 && node build-copy.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
